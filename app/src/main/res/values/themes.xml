<resources>
    <style name="Theme.IChatAI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Core colors -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOutline">@color/colorOutline</item>

        <!-- System bars -->
        <item name="android:statusBarColor">@color/colorBackground</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
    </style>

    <!-- Simple splash theme using your layout background -->
    <style name="Theme.iChatAI.Splash" parent="Theme.IChatAI">
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/white</item>
    </style>

    <style name="DayCircle" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:background">@drawable/circle_unfilled_bg</item>
    </style>
</resources>
